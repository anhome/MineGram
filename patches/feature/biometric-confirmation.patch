From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: alina sireneva <alina@tei.su>
Date: Wed, 1 Jul 2026 18:07:39 +0400
Subject: client-side biometric confirmation in various places

---
 .../src/main/java/org/telegram/ui/ChatActivity.java    | 10 ++++++----
 .../java/org/telegram/ui/Components/AlertsCreator.java |  4 ++--
 .../src/main/java/org/telegram/ui/DialogsActivity.java |  8 ++++----
 .../src/main/java/org/telegram/ui/LogoutActivity.java  |  2 +-
 .../src/main/java/org/telegram/ui/ProfileActivity.java |  8 ++++----
 5 files changed, 17 insertions(+), 15 deletions(-)

diff --git a/TMessagesProj/src/main/java/org/telegram/ui/ChatActivity.java b/TMessagesProj/src/main/java/org/telegram/ui/ChatActivity.java
index 0000000..0000000 100644
--- a/TMessagesProj/src/main/java/org/telegram/ui/ChatActivity.java
+++ b/TMessagesProj/src/main/java/org/telegram/ui/ChatActivity.java
@@ -3822,12 +3822,12 @@ public class ChatActivity extends BaseFragment implements
                         if (getThreadId() != 0) {
                             final TLRPC.User user = getMessagesController().getUser(getThreadId());
                             if (user != null) {
-                                AlertsCreator.createClearDaysDialogAlert(ChatActivity.this, -1, user, currentChat, true, revoke -> {
+                                AlertsCreator.createClearDaysDialogAlert(ChatActivity.this, -1, user, currentChat, true, revoke -> desu.mintgram.helpers.security.BiometricHelper.gate(getParentActivity(), desu.mintgram.InuConfig.BIOMETRIC_CONFIRM_DELETE_CHAT.getValue(), () -> {
                                     if (user.id != getThreadId()) {
                                         return;
                                     }
                                     performHistoryClear(false, true);
-                                }, getResourceProvider());
+                                }), getResourceProvider());
                             }
                         }
                         return;
@@ -3838,6 +3838,7 @@ public class ChatActivity extends BaseFragment implements
                         AlertsCreator.createClearDaysDialogAlert(ChatActivity.this, -1, currentUser, currentChat, canDeleteHistory, new MessagesStorage.BooleanCallback() {
                             @Override
                             public void run(boolean revoke) {
+                                desu.mintgram.helpers.security.BiometricHelper.gate(getParentActivity(), desu.mintgram.InuConfig.BIOMETRIC_CONFIRM_DELETE_CHAT.getValue(), () -> {
                                 if (revoke && (currentUser != null || canDeleteHistory)) {
                                     getMessagesStorage().getMessagesCount(dialog_id, (count) -> {
                                         if (count >= 50) {
@@ -3849,11 +3850,12 @@ public class ChatActivity extends BaseFragment implements
                                 } else {
                                     performHistoryClear(revoke, canDeleteHistory);
                                 }
+                                });
                             }
                         }, getResourceProvider());
                         return;
                     }
-                    AlertsCreator.createClearOrDeleteDialogAlert(ChatActivity.this, id == clear_history, currentChat, currentUser, currentEncryptedChat != null, true, false, canDeleteHistory, (param) -> {
+                    AlertsCreator.createClearOrDeleteDialogAlert(ChatActivity.this, id == clear_history, currentChat, currentUser, currentEncryptedChat != null, true, false, canDeleteHistory, (param) -> desu.mintgram.helpers.security.BiometricHelper.gate(getParentActivity(), desu.mintgram.InuConfig.BIOMETRIC_CONFIRM_DELETE_CHAT.getValue(), () -> {
                         if (id == clear_history && ChatObject.isChannel(currentChat) && (!currentChat.megagroup || ChatObject.isPublic(currentChat))) {
                             getMessagesController().deleteDialog(dialog_id, 2, param);
                         } else {
@@ -3866,7 +3868,7 @@ public class ChatActivity extends BaseFragment implements
                                 performHistoryClear(param, canDeleteHistory);
                             }
                         }
-                    });
+                    }));
                 } else if (id == share_contact) {
                     if (currentUser == null || getParentActivity() == null) {
                         return;
diff --git a/TMessagesProj/src/main/java/org/telegram/ui/Components/AlertsCreator.java b/TMessagesProj/src/main/java/org/telegram/ui/Components/AlertsCreator.java
index 0000000..0000000 100644
--- a/TMessagesProj/src/main/java/org/telegram/ui/Components/AlertsCreator.java
+++ b/TMessagesProj/src/main/java/org/telegram/ui/Components/AlertsCreator.java
@@ -1464,7 +1464,7 @@ public class AlertsCreator {
             }
             reportText = LocaleController.getString(R.string.ReportChat);
         }
-        builder.setPositiveButton(reportText, (dialogInterface, i) -> {
+        builder.setPositiveButton(reportText, (dialogInterface, i) -> desu.mintgram.helpers.security.BiometricHelper.gate(fragment.getParentActivity(), (cells == null || cells[1].isChecked()) && desu.mintgram.InuConfig.BIOMETRIC_CONFIRM_DELETE_CHAT.getValue(), () -> {
             if (currentUser != null) {
                 accountInstance.getMessagesController().blockPeer(currentUser.id);
             }
@@ -1485,7 +1485,7 @@ public class AlertsCreator {
             } else {
                 callback.run(0);
             }
-        });
+        }));
         builder.setNegativeButton(LocaleController.getString(R.string.Cancel), null);
         AlertDialog dialog = builder.create();
         fragment.showDialog(dialog);
diff --git a/TMessagesProj/src/main/java/org/telegram/ui/DialogsActivity.java b/TMessagesProj/src/main/java/org/telegram/ui/DialogsActivity.java
index 0000000..0000000 100644
--- a/TMessagesProj/src/main/java/org/telegram/ui/DialogsActivity.java
+++ b/TMessagesProj/src/main/java/org/telegram/ui/DialogsActivity.java
@@ -9484,7 +9484,7 @@ public class DialogsActivity extends BaseFragment implements NotificationCenter.
             } else {
                 user = null;
             }
-            AlertsCreator.createBlockDialogAlert(DialogsActivity.this, count, canReportSpamCount != 0, user, (report, delete) -> {
+            AlertsCreator.createBlockDialogAlert(DialogsActivity.this, count, canReportSpamCount != 0, user, (report, delete) -> desu.mintgram.helpers.security.BiometricHelper.gate(getParentActivity(), delete && desu.mintgram.InuConfig.BIOMETRIC_CONFIRM_DELETE_CHAT.getValue(), () -> {
                 for (int a = 0, N = selectedDialogs.size(); a < N; a++) {
                     long did = selectedDialogs.get(a);
                     if (report) {
@@ -9497,7 +9497,7 @@ public class DialogsActivity extends BaseFragment implements NotificationCenter.
                     getMessagesController().blockPeer(did);
                 }
                 hideActionMode(false);
-            });
+            }));
             return;
         }
         int minPinnedNum = Integer.MAX_VALUE;
@@ -9581,7 +9581,7 @@ public class DialogsActivity extends BaseFragment implements NotificationCenter.
                         builder.setNegativeButton(LocaleController.getString(R.string.Cancel), null);
                         showDialog(builder.create());
                     } else {
-                         AlertsCreator.createClearOrDeleteDialogAlert(DialogsActivity.this, action == clear, chat, user, DialogObject.isEncryptedDialog(dialog.id), action == delete, false, false, (param) -> {
+                        AlertsCreator.createClearOrDeleteDialogAlert(DialogsActivity.this, action == clear, chat, user, DialogObject.isEncryptedDialog(dialog.id), action == delete, false, false, (param) -> desu.mintgram.helpers.security.BiometricHelper.gate(getParentActivity(), desu.mintgram.InuConfig.BIOMETRIC_CONFIRM_DELETE_CHAT.getValue(), () -> {
                             hideActionMode(false);
                             if (action == clear && ChatObject.isChannel(chat) && (!chat.megagroup || ChatObject.isPublic(chat))) {
                                 getMessagesController().deleteDialog(selectedDialog, 2, param);
@@ -9629,7 +9629,7 @@ public class DialogsActivity extends BaseFragment implements NotificationCenter.
                                     }
                                 }
                             }
-                        });
+                        }));
                     }
                     return;
                 } else {
diff --git a/TMessagesProj/src/main/java/org/telegram/ui/LogoutActivity.java b/TMessagesProj/src/main/java/org/telegram/ui/LogoutActivity.java
index 0000000..0000000 100644
--- a/TMessagesProj/src/main/java/org/telegram/ui/LogoutActivity.java
+++ b/TMessagesProj/src/main/java/org/telegram/ui/LogoutActivity.java
@@ -164,7 +164,7 @@ public class LogoutActivity extends BaseFragment {
         AlertDialog.Builder builder = new AlertDialog.Builder(context);
         builder.setMessage(LocaleController.getString(R.string.AreYouSureLogout));
         builder.setTitle(LocaleController.getString(R.string.LogOut));
-        builder.setPositiveButton(LocaleController.getString(R.string.LogOut), (dialogInterface, i) -> MessagesController.getInstance(currentAccount).performLogout(1));
+        builder.setPositiveButton(LocaleController.getString(R.string.LogOut), (dialogInterface, i) -> desu.mintgram.helpers.security.BiometricHelper.gate(context, desu.mintgram.InuConfig.BIOMETRIC_CONFIRM_LOGOUT.getValue(), () -> MessagesController.getInstance(currentAccount).performLogout(1)));
         builder.setNegativeButton(LocaleController.getString(R.string.Cancel), null);
         AlertDialog alertDialog = builder.create();
         TextView button = (TextView) alertDialog.getButton(DialogInterface.BUTTON_POSITIVE);
diff --git a/TMessagesProj/src/main/java/org/telegram/ui/ProfileActivity.java b/TMessagesProj/src/main/java/org/telegram/ui/ProfileActivity.java
index 0000000..0000000 100644
--- a/TMessagesProj/src/main/java/org/telegram/ui/ProfileActivity.java
+++ b/TMessagesProj/src/main/java/org/telegram/ui/ProfileActivity.java
@@ -6168,7 +6168,7 @@ public class ProfileActivity extends BaseFragment implements NotificationCenter.
             }
         } else {
             if (!userBlocked || fromActions) {
-                AlertsCreator.createClearOrDeleteDialogAlert(ProfileActivity.this, false, currentChat, user, currentEncryptedChat != null, true, false, true, (param) -> {
+                AlertsCreator.createClearOrDeleteDialogAlert(ProfileActivity.this, false, currentChat, user, currentEncryptedChat != null, true, false, true, (param) -> desu.mintgram.helpers.security.BiometricHelper.gate(getParentActivity(), desu.mintgram.InuConfig.BIOMETRIC_CONFIRM_DELETE_CHAT.getValue(), () -> {
                     if (getParentLayout() != null) {
                         List<BaseFragment> fragmentStack = getParentLayout().getFragmentStack();
                         BaseFragment prevFragment = fragmentStack == null || fragmentStack.size() < 2 ? null : fragmentStack.get(fragmentStack.size() - 2);
@@ -6179,7 +6179,7 @@ public class ProfileActivity extends BaseFragment implements NotificationCenter.
                     disableProfileAnimation = true;
                     finishFragment();
                     getNotificationCenter().postNotificationName(NotificationCenter.needDeleteDialog, dialogId, user, currentChat, param);
-                });
+                }));
             } else {
                 getMessagesController().unblockPeer(userId, () -> getSendMessagesHelper().sendMessage(SendMessagesHelper.SendMessageParams.of("/start", userId, null, null, null, false, null, null, null, true, 0, 0, null, false)));
                 finishFragment();
@@ -7659,13 +7659,13 @@ public class ProfileActivity extends BaseFragment implements NotificationCenter.
 
     private void leaveChatPressed(boolean delete) {
         boolean isForum = ChatObject.isForum(currentChat);
-        AlertsCreator.createClearOrDeleteDialogAlert(ProfileActivity.this, false, currentChat, null, false, isForum || delete || (currentChat != null && currentChat.creator), delete, !isForum, (param) -> {
+        AlertsCreator.createClearOrDeleteDialogAlert(ProfileActivity.this, false, currentChat, null, false, isForum || delete || (currentChat != null && currentChat.creator), delete, !isForum, (param) -> desu.mintgram.helpers.security.BiometricHelper.gate(getParentActivity(), desu.mintgram.InuConfig.BIOMETRIC_CONFIRM_DELETE_CHAT.getValue(), () -> {
             playProfileAnimation = 0;
             getNotificationCenter().removeObserver(ProfileActivity.this, NotificationCenter.closeChats);
             getNotificationCenter().postNotificationName(NotificationCenter.closeChats);
             finishFragment();
             getNotificationCenter().postNotificationName(NotificationCenter.needDeleteDialog, -currentChat.id, null, currentChat, param);
-        });
+        }));
     }
 
     private void getChannelParticipants(boolean reload) {
